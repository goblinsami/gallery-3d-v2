Place the brick texture pair here when using the brick material family:

- `brick_color.jpg`
- `brick_normal.jpg`

If either file is missing, Gallery3D V2 falls back to a deterministic procedural brick texture.
